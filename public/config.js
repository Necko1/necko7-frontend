// Optional runtime configuration (e.g. for Docker / production container injection).
// Values set here take precedence over build-time .env variables.
// If left empty or undefined, the application reads from your .env file:
//   VITE_API_BASE_URL
//   VITE_BACKEND_URL

window.__APP_CONFIG__ = {
  // API_BASE_URL: "",
  // BACKEND_URL: "",
};
