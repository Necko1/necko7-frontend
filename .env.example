# Base URL for Axios API calls.
# - In local dev: leave empty to use Vite proxy (/api/*)
# - In production / Docker: set to your backend origin or leave empty if served behind same reverse proxy
API_BASE_URL=

# Backend server origin (where Twitch OAuth callbacks are handled)
BACKEND_URL=https://7.necko.moe
